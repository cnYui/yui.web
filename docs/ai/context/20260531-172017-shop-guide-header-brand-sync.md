# 商店使用说明页导航品牌同步

## 背景

`/shop/guide/` 页面左上角仍显示 `Shop.`，和站点主页及商店首页的 `Portfolio.` 不一致。

## 修改

- 将 `/shop/guide/` 左上角品牌文字从 `Shop.` 改为 `Portfolio.`
- 将品牌链接从 `/shop/` 改为 `/`

## 验证

检查本地文件和公网页面，确认 `/shop/guide/` 已显示 `Portfolio.`，不再显示 `Shop.`。
