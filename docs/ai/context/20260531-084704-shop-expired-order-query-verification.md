# 手机号查询保留过期订单验证

## 改动

- 查询页文案更新，明确说明过期订单会显示为“已失效”，但仍保留在查询结果里。
- 测试增加手工插入过期订单并通过 `/api/orders?phone=...` 查询的场景。

## 验证

- `npm test`：13 个测试全部通过。
- `npm run build:css`：构建完成。
- 本机 `/shop/query/` 能看到过期订单保留说明。
- 公网 `https://aaccx.pw/shop/query/` 能看到同一说明。
- 浏览器验证：查询页展示过期订单保留说明。

## 数据状态

- `orders = 3`，历史订单仍保留。
- `invite_codes` 只剩 `unused = 8`。
